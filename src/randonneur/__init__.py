"""randonneur — a small tool to plot GPX tracks onto a hiking map.

This package is the entry point for the tool. See ``cli`` for the user-facing
commands (``randonneur`` with no args starts the local server) and
``server`` for the FastAPI app the static UI talks to.
"""

__version__ = "0.1.0"
